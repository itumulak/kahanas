# UI Registry

*Purpose: every reusable UI component the project has, so a new feature reuses an existing component instead of building a near duplicate. Optional: only present once the project has a component based UI layer.*

A living inventory of reusable UI components. Check here before building a new component; extend an existing one instead of duplicating it wherever possible.

---

## <COMPONENT_NAME> (`<FILE_PATH>`)

*Purpose: what the component does, its props, and one small real usage example, so an agent can drop it into a new page without reading its source. Repeat this whole section once per reusable component worth registering.*

**Used for**: <WHAT_ITS_USED_FOR>

**Props**:
| Prop | Type | Notes |
| --- | --- | --- |
| <PROP_NAME> | <PROP_TYPE> | <NOTE> |

```<LANGUAGE>
<SHORT_REAL_USAGE_EXAMPLE>
```

Repeat one `## <COMPONENT_NAME>` section per reusable component.
